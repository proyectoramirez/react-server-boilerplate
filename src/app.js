import React from 'react';
import ReactDOM from 'react-dom';

const MOUNT_NODE = document.getElementById("app");

ReactDOM.render(
    <div>
        <h1>HELLO BUDDIES</h1>
        <button>HOLI</button>
    </div>,
    MOUNT_NODE
);